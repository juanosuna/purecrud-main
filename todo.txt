check db schema for poorly named artifacts

Security:
roles/permissions
admin, create users, roles, reference entities
active/deactivate users
pw reset
registration

Audit trail

Excel export

Add attachments

I18N, localization
user-specific locale

Dashboard
Last Viewed
Favorites
Remember last search

UI Design Improvements:
Check fonts, color scheme and layout with Elena
Change tabs when selected, e.g. make a bit bigger
Improve icons, e.g. expand/collapse one

Maven:
mvn deploy:deploy-file -Durl=scp://www.brownbagconsulting.com -DrepositoryId=brownbag -Dfile=sample-web.war
